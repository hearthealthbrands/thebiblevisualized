<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>{{ title or "The Bible Visualized" }}</title>
    
    <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
    
    <meta name="view-transition" content="same-origin" />
    
    <meta property="og:title" content="{{ title }}" />
    <meta property="og:description" content="{{ verse_text or 'Daily scripture visualized through AI art.' }}" />
    <meta property="og:type" content="article" />
    <meta property="og:url" content="https://thebiblevisualized.com{{ page.url }}" />
    <meta property="og:image" content="https://thebiblevisualized.com{{ image }}" />
    
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:site" content="@TheBibleViz"> <meta name="twitter:image" content="https://thebiblevisualized.com{{ image }}">

    <link rel="stylesheet" href="/css/style.css">
</head>

{% set previousPost = collections.post | getPreviousCollectionItem(page) %}
{% set nextPost = collections.post | getNextCollectionItem(page) %}

<body 
    data-prev="{{ previousPost.url }}" 
    data-next="{{ nextPost.url }}"
>

    <header>
        <nav>
            <a href="/" class="logo-text">
                The Bible <em>Visualized</em>
            </a>
            
            <div class="sub-nav">
                <a href="/">Gallery</a>
                <a href="/about">About</a>
            </div>
        </nav>
    </header>

    <main>
        {{ content | safe }}
    </main>

    <footer>
        
        <div class="nav-links" style="margin-bottom: 40px; display: flex; justify-content: center; gap: 20px; font-family: var(--font-sans); font-size: 0.9rem;">
            {% if previousPost %}
                <a href="{{ previousPost.url }}" style="border-bottom: 1px solid var(--accent);">← Previous</a>
            {% endif %}
            
            {% if nextPost %}
                <a href="{{ nextPost.url }}" style="border-bottom: 1px solid var(--accent);">Next Verse →</a>
            {% endif %}
        </div>

        {% include "components/subscribe.njk" %}

        <hr style="margin: 30px auto; width: 50%; opacity: 0.3;">

        <button id="share-btn">Share this Page</button>
        
        <div style="margin-top: 30px; display: flex; flex-direction: column; gap: 10px; align-items: center;">
            
            <a href="mailto:hello@thebiblevisualized.com" style="font-size: 0.9rem; color: var(--accent); text-decoration: none;">
                hello@thebiblevisualized.com
            </a>

            <a href="https://x.com/TheBibleViz" target="_blank" rel="noopener noreferrer" style="font-size: 0.9rem; color: var(--text-muted); text-decoration: none; border-bottom: 1px dotted var(--text-muted);">
                Follow @TheBibleViz
            </a>
            
        </div>

        <div style="margin-top: 20px;">
            <p>&copy; <span id="copyright-year">2025</span> The Bible Visualized</p>
            <p>Visualizing the Word, one verse at a time.</p>
        </div>
    </footer>

    <script src="/js/script.js"></script>
</body>
</html>